---
sidebar: sidebar
permalink: mcp-server-get-started.html
keywords: 
summary: 
---
= Get started with the docs.netapp.com MCP server
:hardbreaks:
:nofooter:
:icons: font
:linkattrs:
:imagesdir: ./media/

[.lead]
Connect your AI development environment, such as Visual Studio Code with GitHub Copilot, Claude Desktop, or custom Python agents, to the docs.netapp.com Model Context Protocol (MCP) server. After you've configured the MCP server, use it to return grounded answers from published NetApp documentation.

== Prerequisites

Before configuring the docs.netapp.com MCP server, verify the following:

* You have reviewed the requirements and limitations in link:learn-mcp-server.html[Learn about the docs.netapp.com MCP server].
* You have access to a supported MCP-compliant client such as Visual Studio Code with GitHub Copilot, Claude Desktop, or Python-based agent frameworks.
* If you require external client access, you must have an active *NetApp APIM subscription key*. To request a key, contact the IE development team.
* You have network access to the docs.netapp.com MCP endpoint: `\https://docs-stage.netapp.com/mcp`.

== Configure your development environment

Choose the setup option for your environment.

* <<Visual Studio Code setup>>
* <<Python AI agent integration>>
* <<Claude Desktop configuration>>



=== Visual Studio Code setup

You can connect to the MCP server from Visual Studio Code using GitHub Copilot by configuring an `mcp.json` file.

.Before you begin

* This task requires Visual Studio Code with the GitHub Copilot extension enabled.

.Steps

. From your project workspace in Visual Studio Code, complete the following:

.. Create a folder named `.vscode`
.. Create a file within the folder called `mcp.json`

. Configure the `mcp.json` file depending on whether you want to configure remote, local or external access to the MCP server. 
+
[role="tabbed-block"]
====
.Remote MCP Server (Recommended)
--
Use this option when you are on the NetApp corporate network or VPN. 

[source,json]
----
{
  "servers": {
    "netapp-docs-mcp": {
      "type": "http",
      "url": "https://docs-stage.netapp.com/mcp"
    }
  }
}
----

NOTE: Access to this endpoint is restricted. Ensure your device is connected to the NetApp corporate network (for example, via VPN) before attempting to connect.

--
.Local MCP server
--
Use this option when you want to connect to a local MCP service.

[source,json]
----
{
  "servers": {
    "netapp-docs-mcp": {
      "type": "http",
      "url": "http://localhost:8000"
    }
  }
}
----

--
.External access (non-VPN users)
--
Use this option when you need to connect from outside the NetApp corporate network.  

NOTE: This option requires an API subscription key. If you do not already have a key, contact the IE development team to request access.

[source,json]
----
{
  "servers": {
    "netapp-docs-mcp": {
      "type": "http",
      "url": "https://docs-stage.netapp.com/mcp",
      "headers": {
        "Ocp-Apim-Subscription-Key": "${input:Ocp-Apim-Subscription-Key}"
      }
    }
  },
  "inputs": [
    {
      "type": "promptString",
      "id": "Ocp-Apim-Subscription-Key",
      "description": "Enter your access key",
      "password": true
    }
  ]
}
----

--
====

. Save the file. 

. Optionally, configure global access. 
+
To configure the docs.netapp.com MCP server across all Visual Studio Code projects and workspaces, add the configuration to your global user settings directory.
+
* *Windows*: `C:\Users\<your_username>\AppData\Roaming\Code\User\mcp.json`
* *macOS*: `~/Library/Application Support/Code/User/mcp.json`
+
Add the server definition:
+
[source,json]
----
{
  "servers": {
    "netapp-docs-mcp": {
      "type": "http",
      "url": "https://docs-stage.netapp.com/mcp"
    }
  }
}
----
+
NOTE: Restart Visual Studio Code after modifying global `mcp.json` files for the configuration to take effect.

. Verify the configuration.
.. Open Visual Studio Code in your workspace.
.. Confirm that the `.vscode/mcp.json` file is present.
.. Ask a question using Copilot. 
+
.Example
+
_What are the differences between ONTAP and Cloud Volumes ONTAP?_

=== Python AI agent integration

For custom AI applications or Python agent pipelines, connect directly using an MCP client library:

[source,python]
----
from mcp import MCPClient

# Initialize connection to the NetApp Docs MCP Server
client = MCPClient("https://docs-stage.netapp.com/mcp")

# Call the RAG answer tool
response = client.call_tool("netapp_docs_ask", {
    "question": "What are the steps to perform a non-disruptive ONTAP upgrade?"
})

print(response)
----

=== Claude Desktop configuration

To register the docs.netapp.com MCP server with Claude Desktop, update your configuration file:

* *macOS*: `~/Library/Application Support/Claude/claude_desktop_config.json`
* *Windows*: `%APPDATA%\Claude\claude_desktop_config.json`

[source,json]
----
{
  "mcpServers": {
    "netapp-docs-mcp": {
      "command": "npx",
      "args": [
        "mcp-remote",
        "https://docs-stage.netapp.com/mcp"
      ]
    }
  }
}
----


==  Use the docs.netapp.com MCP server

After you've configured the MCP server, you can interact with NetApp technical documentation directly inside Visual Studio Code or your chosen client interface. 

.About this task

* Start with `netapp_docs_ask` for most questions.
* Use `netapp_docs_search` for keyword-based discovery.
* Use `netapp_docs_fetch` to retrieve detailed document content.
* Use clear, complete questions instead of short or vague queries.
* Responses are always grounded in official NetApp documentation.

NOTE: MCP tools are independent, there is no required execution flow. However, it is recommended to start with `netapp_docs_ask` for most scenarios. This provides the fastest and most complete results.

.Steps

. Open Copilot Chat or your preferred MCP-compatible client.
. Ask a clear question about NetApp products or procedures.
. Review the grounded response.

.Related information

* link:mcp-developer-reference.html[Developer reference]

