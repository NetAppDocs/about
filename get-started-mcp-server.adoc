---
sidebar: sidebar
permalink: get-started-mcp-server.html
keywords: 
summary: 
---
= Get started with the docs.netapp.com MCP server
:hardbreaks:
:nofooter:
:icons: font
:linkattrs:
:imagesdir: ./media/

[.lead]
Connect your AI development environment, such as Visual Studio Code with GitHub Copilot, Claude Desktop, or custom Python agents, to the docs.netapp.com Model Context Protocol (MCP) server.

== Before you begin

Before configuring the NetApp Docs MCP Server, verify the following:

* You have reviewed the information in link:learn-mcp-server.html[Learn about the docs.netapp.com MCP server].
* Visual Studio Code is installed with the GitHub Copilot extension enabled.
* If you require external client access, you must have an active *NetApp APIM Subscription Key*.
* You have network access to the docs.netapp.com MCP endpoint: `https://docs-stage.netapp.com/mcp` (or production endpoint `https://docs.netapp.com/mcp`).

== High-Level Workflow

Integrating and using the MCP server involves four primary steps:

. Configure your client application or development environment.
. Connect to the NetApp Docs MCP server endpoint.
. Query NetApp documentation using interactive natural language commands.
. (Recommended) Configure system instructions to optimize tool selection by AI agents.

== Step 1: Configure your development environment

Choose the setup option that matches your environment.

[role="tabbed-block"]
====
.Local VS Code workspace setup
--
Use this option to enable the NetApp Docs MCP Server within a specific Visual Studio Code workspace.

.Steps
. Create a configuration file in your workspace directory at `.vscode/mcp.json`.
. Add the following server configuration:

[source,json]
----
{
  "servers": {
    "netapp-docs-mcp": {
      "type": "http",
      "url": "https://docs-stage.netapp.com/mcp"
    }
  }
}
----

. Save the file. When prompted by Visual Studio Code, enter your NetApp APIM Subscription Key.
--
.Global VS Code setup (all workspaces)
--
To make the NetApp Docs MCP Server available across all Visual Studio Code projects and workspaces, add the configuration to your global user settings directory.

* *Windows*: `C:\Users\<your_username>\AppData\Roaming\Code\User\mcp.json`
* *macOS*: `~/Library/Application Support/Code/User/mcp.json`

Add the server definition:

[source,json]
----
{
  "servers": {
    "netapp-docs-mcp": {
      "type": "http",
      "url": "https://docs-stage.netapp.com/mcp"
    }
  }
}
----

IMPORTANT: Restart Visual Studio Code after modifying global `mcp.json` files for the configuration to take effect.

--
.Claude desktop configuration
--
To register the NetApp Docs MCP Server with Claude Desktop, update your configuration file:

* *macOS*: `~/Library/Application Support/Claude/claude_desktop_config.json`
* *Windows*: `%APPDATA%\Claude\claude_desktop_config.json`

[source,json]
----
{
  "mcpServers": {
    "netapp-docs-mcp": {
      "command": "npx",
      "args": [
        "mcp-remote",
        "https://docs-stage.netapp.com/mcp"
      ]
    }
  }
}
----

--
.Python AI agent integration
--
For custom AI applications or Python agent pipelines, connect directly using an MCP client library:

[source,python]
----
from mcp import MCPClient

# Initialize connection to the NetApp Docs MCP Server
client = MCPClient("https://docs-stage.netapp.com/mcp")

# Call the RAG answer tool
response = client.call_tool("netapp_docs_ask", {
    "question": "What are the steps to perform a non-disruptive ONTAP upgrade?"
})

print(response)
----

--
====

== Step 2: Use the MCP Server

Once configured, you can interact with NetApp technical documentation directly inside Visual Studio Code or your chosen client interface.

. Open **Copilot Chat** in Visual Studio Code.
. Select **Agent Mode**.
. Ask questions relevant to NetApp storage management, product features, or troubleshooting.

=== Recommended Tool Selection

The NetApp Docs MCP Server exposes three tools. Understanding when to use each ensures optimal performance:

`netapp_docs_ask` (Recommended Default)::
Generates complete, synthesized answers grounded in official documentation with reference citations. Use for how-to questions, conceptual explanations, and troubleshooting.
+
* *Configuration / How-To*: "How do I configure SnapMirror replication between two ONTAP clusters?"
* *Troubleshooting*: "SnapMirror replication is failing — how do I resolve connection errors?"
* *Conceptual*: "What is WAFL and how does write allocation work in ONTAP?"
* *Comparisons*: "What is the key difference between SnapMirror and SnapVault?"

`netapp_docs_search`::
Discovers relevant documentation articles based on keyword queries. Use during early research or topic discovery.
+
* *Examples*: `ONTAP upgrade`, `SnapMirror configuration`, `SVM peering`, `NFS setup ONTAP`

`netapp_docs_fetch`::
Retrieves full, structured content for a specific article URL returned by search. Use when step-by-step procedures or exhaustive technical details are required.

== Step 3: set custom agent instructions (Recommended)

To ensure your AI agent consistently utilizes the NetApp Docs MCP Server when handling NetApp-related queries, add custom instructions to your workspace.

1. Open Copilot Chat in Visual Studio Code and select **Agent Mode**.
2. Click the settings gear icon at the top of the chat window and select **Instructions**.
3. Create or edit `.github/copilot-instructions.md` with the following configuration:

[source,markdown]
----
---
applyTo: '**'
---
## Querying NetApp Technical Documentation

You have access to NetApp Docs MCP tools: `netapp_docs_ask`, `netapp_docs_search`, and `netapp_docs_fetch`. These tools provide access to official, up-to-date NetApp product documentation.

When answering questions about NetApp technologies (including ONTAP, MetroCluster, SnapMirror, SnapCenter, FabricPool, and NetApp REST APIs):
- Always prefer `netapp_docs_ask` as the primary tool to retrieve grounded answers and citations.
- Use `netapp_docs_search` for keyword discovery when specific article links are required.
- Use `netapp_docs_fetch` to extract full procedural text from article URLs.
----

=
